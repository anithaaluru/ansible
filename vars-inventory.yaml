- name: giving vars by prompt
  hosts: local
  connection: local
  tasks:
   - name: print course information
     ansible.builtin.debug:
      msg: "hello, my course is {{ COURSE }}, duration is {{ DURATION }}, Trainer is {{ TRAINER }}"
        
