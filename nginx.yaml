- name: install and start nginx
  hosts: frontend
  tasks:
  - name: install the nginx
    ansible.builtin.service:
      name: nginx
      state: present
  - name: start the nginx
    ansible.builtin.service:
      name: nginx
      state: started
