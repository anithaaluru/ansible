- name: installing and starting nginx
  hosts: frontend
  tasks:
  - name: install the nginx
    ansible.builtin.service:
      name: nginx
      state: installed

  - name: start the nginx
    ansible.builtin.service:
      name: nginx
      state: started
