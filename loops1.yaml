- name: playbook using loops
  hosts: frontend
  become: yes
  tasks:
  - name: install package
    ansible.builtin.package:
    - name: "{{ item }}"
      state: present
    loop:
    - nginx
    - mysql
    - zip